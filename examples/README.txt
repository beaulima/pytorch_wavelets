Examples
========

Worked examples, each one a script under ``examples/`` that is executed every
time the documentation is built - so the figures below always come from the
code as it stands, and an example that stops working breaks the build.

Every page can be downloaded as a Python script or as a Jupyter notebook from
the links at its foot.
